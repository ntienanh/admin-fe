import { Layout } from 'antd';
const { Header } = Layout;
const HeaderMainLayout = () => {
  return <Header className='fixed top-0 h-[64px] w-full bg-white !px-4 shadow-md'>12312 </Header>;
};

export default HeaderMainLayout;
